# Copy to .env and fill in. .env is gitignored — this repo is public.

# CBE API key from https://cbeapi.be
# The original key in this project was named CBEAPI-RIXTESTKEY; hyphens are not
# valid shell identifiers, so CBE_API_KEY is preferred. Both are accepted.
CBE_API_KEY=

# Neo4j password for the local container.
NEO4J_PASSWORD=openabox-local
